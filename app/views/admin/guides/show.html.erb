<h2>Viewing Edition <%= @latest_edition.version_number %> of &ldquo;<%= @guide.title %>&rdquo;</h2>
<p>Status: <%= @latest_edition.latest_action %></p>

<% if @latest_edition.in_review? %>
  <%= render 'review' %>
<% end %>

<div id="guide-controls">
  <!-- <p><a href="#" class="external">Formatting help</a></p> -->
  <span class="workflow_buttons">

  <% if @latest_edition.is_published? %>
    Published guides can't be changed.
    <%= preview_button(@latest_edition) %>
    <%= button_to 'Create new edition', admin_guide_editions_path(@guide) %>
  <% else %>
    <!-- Fake save button -->
    <form id="save-edition" class="button_to">
    <input type="submit" value="Save" class="update">
    </form>
    <%= progress_buttons(@guide,@latest_edition) %>
    <%= preview_button(@latest_edition) %>
  <% end %>
  </span>
    <%= progress_forms(@guide,@latest_edition) %>
</div>

<div id="guide-nav" class="publication-nav">
  <ul>
    <li><a href="#edit">Edit</a></li>
    <li><a href="#metadata">Metadata</a></li>
    <li><a href="#history">History</a></li>
  </ul>
  <div id="edit">
    <%= render '/admin/guides/edit' %>
  </div>
  <div id="metadata">
    <%= render :partial => '/admin/shared/metadata', :locals => {:publication => @guide} %>
  </div>
  <div id="history">
    <%= render :partial => '/admin/shared/history', :locals => {:publication => @guide} %>
  </div>
</div>

<% content_for :extra_javascript, javascript_include_tag('/guide-assets/javascripts/publications', '/guide-assets/javascripts/guides') %>
<% content_for :page_title, "Editing #{@guide.title}" %>