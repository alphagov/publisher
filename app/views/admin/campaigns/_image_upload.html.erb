<tr id="<%= format %>-campaign-image">
  <th scope="row"><%= format.capitalize %> image <span class="image-size"><%= size %></span></th>
  <td>
    <% if @edition.send("#{format}_image") %>
      <% if @edition.send("#{format}_image").state == "unscanned" %>
        <p class="alert"><%= @edition.send("#{format}_image").name %> is being scanned for viruses</p>
      <% else %>
        <p>
          <%= link_to @edition.send("#{format}_image").file_url do %>
            <%= image_tag @edition.send("#{format}_image").file_url, :height => "200px" %>
          <% end %>
        </p>
      <% end %>
      <p><%= label_tag do %>Remove image? <%= check_box_tag "edition[remove_#{format}_image]", "1" %><% end %></p>
    <% end %>
    <%= f.input "#{format}_image".to_sym, :as => :file, :label => "Upload image:" %>
  </td>
</tr>
