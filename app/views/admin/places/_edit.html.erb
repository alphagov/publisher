<%= semantic_form_for @latest_edition, :url => admin_place_edition_path(@place, @latest_edition), :as => :edition do |f| %>
  <%= f.inputs do %>
    <%= render :partial => 'admin/shared/title_etc', :locals => {:f => f} %>
    <%= f.input :place_type, :hint => "This is the 'slug' assigned in the imminence app" %>
    <%= f.input :introduction, :as => :text, :input_html => {:rows => 2} %>
    <%= f.input :more_information, :as => :text %>
    <%= f.input :expectation_ids, :label => "Expectations", :as => :check_boxes, :collection => PlaceEdition.expectation_choices %>
  <% end %>
<% end %>
