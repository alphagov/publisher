<h2>Viewing Edition <%= @latest_edition.version_number %> of &ldquo;<%= @place.title %>&rdquo;</h2>
<% if @latest_edition.in_review? %>
  <%= render '/admin/guides/review' %>
<% end %>
<div id="guide-controls">
  <!-- <p><a href="#" class="external">Formatting help</a></p> -->
  <span class="workflow_buttons">

  <!-- Fake save button -->
  <form id="save-edition" class="button_to">
  <input type="submit" value="Save" class="update">
  </form>
  
  <%= progress_buttons(@place,@latest_edition) %>
  <%= preview_button(@latest_edition) %>
  </span>
  <%= progress_forms(@place,@latest_edition) %>
</div>

<div id="place-nav" class="publication-nav">
  <ul>
    <li><a href="#edit">Edit</a></li>
    <li><a href="#metadata">Metadata</a></li>
    <li><a href="#history">History</a></li>
  </ul>
  <div id="edit">
    <%= render '/admin/places/edit' %>
  </div>
  <div id="metadata">
    <%= render :partial => '/admin/shared/metadata', :locals => {:publication => @place} %>
  </div>
  <div id="history">
    <%= render :partial => '/admin/shared/history', :locals => {:publication => @place} %>
  </div>
</div>

<% content_for :extra_javascript, javascript_include_tag('/guide-assets/javascripts/publications') %>