<div class="ui-widget-content new">
  <div class="inner">

    <h1>Create new place thing</h1>

    <%= semantic_form_for [:admin, @place] do |f| %>
      <%= f.inputs do %>
        <%= f.input :name, :hint => true %>
        <%= f.input :slug, :hint => true %>
        <%= f.input :tags, :hint => true %>
      <% end %>

      <%= f.buttons %>
    <% end %>

    <% content_for :extra_javascript do %>
    <script type="text/javascript" charset="utf-8">
      $(function () {
        $('#place_name').change(function () {
          if ($('#place_slug').text() == '') {
            var title = $('#place_name').val();
            $('#place_slug').val(GovUKGuideUtils.convertToSlug(title));
          }
        });
      });
    </script>
    <% end %>
  </div>
</div>

<% content_for :page_title, "Create new place" %>