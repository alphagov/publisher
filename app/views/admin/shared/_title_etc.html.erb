<%= hidden_field_tag "return_to", params[:return_to] if params[:return_to] %>

<label for="whole_edition_assigned_to_id">Assigned to</label>
<%= f.collection_select :assigned_to_id, User.order_by([[:name, :asc]]).all, :id, :name, :prompt => "Select someone" %>

<%= f.input :alternative_title,
      :label => 'Alternative title',
      :input_html => { :class => "span7", :autofocus => true, :disabled => @resource.published? } %>
<i class="icon-question-sign" rel="popover" data-content="Use this where longer title is needed for SEO purposes" data-original-title="Help"></i>

<%= f.input :overview,
      :as => :text,
      :label => 'Meta tag description',
      :input_html => { :class => "span7", :rows => 3, :disabled => @resource.published? } %>
<i class="icon-question-sign" rel="popover" data-content="Some search engines will display this if they can&rsquo;t find what they need in the main text" data-original-title="Help"></i>