<%= f.input :video_url, :label => "Video URL", :hint => 'Put the YouTube URL in here if it exists', :input_html => { :disabled => @latest_edition.is_published? } %>
<%= f.input :video_summary, :label => "Video Summary", :as => :text, :input_html => {:rows => 2, :disabled => @latest_edition.is_published? } %>