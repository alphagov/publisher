<%= f.input :video_url, :label => "Video URL", :hint => 'Put the YouTube URL in here if it exists', :input_html => { :disabled => @resource.published? } %>
<%= f.input :video_summary, :label => "Video Summary", :as => :text, :input_html => {:rows => 2, :disabled => @resource.published? } %>