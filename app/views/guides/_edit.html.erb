<%= semantic_bootstrap_nested_form_for @resource, :url => edition_path(@resource), :as => :edition do |f| %>
  <%= f.inputs do %>
    <%= render :partial => 'shared/title_etc', :locals => {:f => f} %>
    <%= render :partial => 'shared/video', :locals => {:f => f} %>
  <% end %>

  <hr>

  <div class="row-fluid">

    <div class="span8">

      <h3>Parts</h3>

      <section class="accordion" id="parts">

        <%= f.semantic_fields_for :parts, @ordered_parts do |part| %>
          <%= render :partial => '/shared/part', :locals => {:f => part, :editable => ! @resource.locked_for_edits? } %>
        <% end %>
      </section>

      <p><%= f.link_to_add "Add new part", :parts, :data => { :target => "#parts" } %></p>

    </div>

    <div class="span4">
      <h3>Govspeak help</h3>

      <div class="well">
        <%= render "../shared/govspeak_help" %>
      </div>
    </div>

  </div>
<% end %>
