New version of "<%= @publication.title %>" (<%= @publication.class.to_s.humanize %>) created by <%= @action.requester.name %> at <%=Time.now.strftime("%R on %d/%m/%Y") %>

<%= "#{Plek.current.find("publication-preview")}/#{@action.edition.container.slug}?edition=#{@action.edition.version_number}" %>