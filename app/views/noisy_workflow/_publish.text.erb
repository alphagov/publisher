<%= @action.requester.name %> published "<%= @publication.title %>" (<%= @publication.class.to_s.humanize %>) at <%=Time.now.strftime("%R on %d/%m/%Y") %>

<%= "#{Plek.current.find("publication-preview")}/#{@action.edition.container.slug}" %>