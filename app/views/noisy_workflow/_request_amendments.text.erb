"<%= @action.edition.title %>" (<%= @action.edition.kind %>) reviewed by <%= @action.requester.name %> at <%=Time.now.strftime("%R on %d/%m/%Y") %>

Further amends required:
<%= @action.comment %>

<%= "#{Plek.current.find("publication-preview")}/#{@action.edition.slug}?edition=#{@action.edition.version_number}" %>