"<%= @publication.title %>" (<%= @publication.class.to_s.humanize %>) has been assigned to <%= @action.recipient.name %> at <%=Time.now.strftime("%R on %d/%m/%Y") %>

<%= "#{Plek.current.find("frontend")}/#{@action.edition.container.slug}?edition=#{@action.edition.version_number}" %>