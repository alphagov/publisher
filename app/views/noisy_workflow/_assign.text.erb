"<%= @action.edition.title %>" (<%= @action.edition.kind %>) has been assigned to <%= @action.recipient.name %> at <%=Time.now.strftime("%R on %d/%m/%Y") %>

<%= "#{Plek.current.find("publication-preview")}/#{@action.edition.slug}?edition=#{@action.edition.version_number}" %>