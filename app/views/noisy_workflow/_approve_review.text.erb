<%= @action.requester.name %> okayed "<%= @action.edition.title %>" (<%= @action.edition.kind %>) at <%=Time.now.strftime("%R on %d/%m/%Y") %>

Comment: <%= @action.comment %>

<%= "#{Plek.current.find("publication-preview")}/#{@action.edition.slug}?edition=#{@action.edition.version_number}" %>