<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p><%= @publication.title %></p>
    <p><%= @action.friendly_description %></p>
    <p>
    <% if @action.friendly_description == 'Published.' %>
      <a href="<%= "#{Plek.current.find("frontend")}/#{@action.edition.container.slug}" %>">View it live</a>
    <% else %>
      <a href="<%= "#{Plek.current.find("frontend")}/#{@action.edition.container.slug}?edition=#{@action.edition.version_number}" %>">Preview it</a>
    <% end %>
    </p>
  </body>
</html>
