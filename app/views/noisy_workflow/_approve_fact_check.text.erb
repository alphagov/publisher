<%= @action.requester.name %> approved fact check "<%= @publication.title %>" (<%= @publication.class.to_s.humanize %>) at <%=Time.now.strftime("%R on %d/%m/%Y") %>

Comment: <%= @action.comment %>

<%= "#{Plek.current.find("publication-preview")}/#{@action.edition.container.slug}?edition=#{@action.edition.version_number}" %>