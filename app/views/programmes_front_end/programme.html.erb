<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Programme to <%= programme.title %> - www.gov.uk</title>
  <link rel="stylesheet" type="text/css" href="<%= asset_host %>/stylesheets/publisher.css" />
  <% if programme.overview and programme.overview != '' %>
  <meta name="description" content="<%= programme.overview %>">
  <% end %>
	<script src="<%= asset_host %>/javascripts/jquery-1.5.1.min.js" type="text/javascript"></script>
	<script src="<%= asset_host %>/javascripts/jquery.tabs.js" type="text/javascript"></script>
	<script src="<%= asset_host %>/javascripts/jquery.history.js" type="text/javascript"></script>
	<script src="<%= asset_host %>/javascripts/programmes.js" type="text/javascript"></script>
</head>
<body>

<div id="wrapper" class="programme">

  <section role="main" class="group">

    <header class="group">

        <hgroup>
          <h1><span>programme to </span><%= programme.title %></h1>
        </hgroup>

    </header>

		<div class="article-and-navigation group">

			<nav class="programme-switcher group" role="navigation">
				<ul>
					<li class="active"><a href="<%= programme_path(programme.slug, programme.parts.first.slug) %>">About and how to claim</a></li>
					<li><a href="<%= programme_path(programme.slug, programme.parts.last.slug) %>"><%= programme.parts.last.title %></a></li>
				</ul>
			</nav>

			<nav class="programme-progression group" role="navigation">
				<ol>
	        <li class="active"><a href="#overview">Overview<span class="progressor"></span></a></li>
	        <li><a href="#what-youll-get">What you'll get<span class="progressor"></span></a></li>
	        <li><a href="#eligibility">Eligibility<span class="progressor"></span></a></li>
	        <li><a href="#how-to-claim">How to claim</a></li>
				</ol>
			</nav>

			<% if part == programme.parts.last %>
			<article role="article" class="group">
				<div class="inner">
					<header>	
				   	<h1><%= programme.parts.last.title %></h1>
					</header>
				   <%= Govspeak::Document.new(programme.parts.last.body).to_html %>
				</div>
			</article>
			 <% elsif part == programme.parts.first %>
			   <% programme.parts.each do |part| %>
			     <% unless part == programme.parts.last %>
					<article role="article" class="group" id="<%= part.slug %>">
						<div class="inner">
							<header>
			       		<h1><%= part.title %></h1>
							</header>
			       <%= Govspeak::Document.new(part.body).to_html %>
						</div>
					</article>
			     <% end %>
			   <% end %>
			 <% end %>

		</div>

  </section>

</div> <!-- end wrapper -->

</body>
</html>
