<%
  title = @assign == "assignee" ? "Assign person" : "Assign 2i reviewer"
  items = @assign == "assignee" ? available_assignee_items(@resource) : available_reviewer_items(@resource)
  url = @assign == "assignee" ? update_assignee_edition_path(@resource) : update_reviewer_edition_path(@resource)
  name = @assign == "assignee" ? "assignee_id" : "reviewer_id"
%>

<% content_for :title_context, @resource.title %>
<% content_for :page_title, title %>
<% content_for :title, title %>

<%= form_for @resource, url: url do %>
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <%= render "govuk_publishing_components/components/radio", {
          name: name,
          heading: "Choose a person to assign",
          visually_hidden_heading: true,
          items: items
        } %>
    </div>

    <div class="govuk-grid-column-one-third options-sidebar">
      <div class="sidebar-components">
        <%= sidebar_options_heading %>

        <%= sidebar_items_list(edit_assignee_buttons(@resource)) %>
      </div>
    </div>
  </div>
<% end %>
