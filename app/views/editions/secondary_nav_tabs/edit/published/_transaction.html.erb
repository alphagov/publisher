<%= render partial: "editions/secondary_nav_tabs/edit/published/components/title", locals: { edition: @resource } %>

<%= render partial: "editions/secondary_nav_tabs/edit/published/components/meta_tag_description", locals: { edition: @resource } %>

<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "Introduction",
    heading_level: 3,
    font_size: "s",
  } %>
  <p>
    <%= edition.introduction != "" ? edition.introduction : "None added" %>
  </p>
</section>

<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "Start button text",
    heading_level: 3,
    font_size: "s",
  } %>
  <p>
    <%= edition.start_button_text %>
  </p>
</section>

<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "Text below the start button (optional)",
    heading_level: 3,
    font_size: "s",
  } %>
  <p>
    <%= edition.will_continue_on != "" ? edition.will_continue_on : "None added" %>
  </p>
</section>

<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "Link to start of transaction",
    heading_level: 3,
    font_size: "s",
  } %>
  <p>
    <%= edition.link != "" ? edition.link : "None added" %>
  </p>
</section>

<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "More information (optional)",
    heading_level: 3,
    font_size: "s",
  } %>
  <p>
    <%= edition.more_information != "" ? edition.more_information : "None added" %>
  </p>
</section>

<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "Other ways to apply (optional)",
    heading_level: 3,
    font_size: "s",
  } %>
  <p>
    <%= edition.alternate_methods != "" ? edition.alternate_methods : "None added" %>
  </p>
</section>

<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "What you need to know (optional)",
    heading_level: 3,
    font_size: "s",
  } %>
  <p>
    <%= edition.need_to_know != "" ? edition.need_to_know : "None added" %>
  </p>
</section>