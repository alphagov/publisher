<section>
  <%= render "govuk_publishing_components/components/heading", {
    text: "Body",
    heading_level: 3,
    font_size: "s",
  } %>

  <%= edition.body != "" ? simple_format(edition.body) : "None added" %>
</section>
