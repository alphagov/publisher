<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= guide.title %> - gov.uk</title>
  <link rel="stylesheet" type="text/css" href="/stylesheets/guides.css" />
</head>
<body>

<div id="wrapper">

  <section role="main" class="group">

    <article>
      <header class="group">
        <hgroup>
          <h1><a href="#" rel="bookmark" title="Permanent link to <%= guide.title %>"><%= guide.title %></a></h1>
          <p class="modified-date">Last updated: <%= guide.updated_at %></p>
        </hgroup>

        <div class="related">
          <h2>Related tools &amp; topics</h2>
          <ul>
            <li><a href="#">Lorem ipsum</a></li>
            <li><a href="#">Lorem ipsum</a></li>
            <li><a href="#">Lorem ipsum</a></li>
            <li><a href="#">Lorem ipsum</a></li>
          </ul>
        </div>

      </header>

	    <aside>
	      <nav>
	        <ol>
	          <% guide.parts.each do |nav_part| %>
	          <li <% if part == nav_part %>class="active"<% end %>>
	            <a href="<%= guide_path(guide.slug, nav_part.slug) %>">
	              <p class="step">
	                <span><%= nav_part.number %></span>
	              </p>
	              <h3><%= nav_part.title %></h3>
	            </a>
	          </li>
	          <% end %>
	        </ol>
	      </nav>
	    </aside>

      <h2><a href="#" rel="bookmark" title="Permanent link to part.title">Part.title</a></h2>

      <nav class="pagination">
        <%- if guide.has_previous_part?(part) -%>
        <p class="previous"><a href="<%= guide_path(guide.slug, guide.part_before(part).slug) %>">&larr; Previous section</a></p>
        <%- end -%>
        <p class="pages">Part <%= part.number %> of <%= guide.parts.length %> in this guide.</p>
        <%- if guide.has_next_part?(part) -%>
        <p class="next"><a href="<%= guide_path(guide.slug, guide.part_after(part).slug) %>">Next section &rarr;</a></p>
        <%- end -%>
      </nav>

      <%= Govspeak::Document.new(part.body).to_html %>

      <footer>
	      <nav class="pagination">
	        <%- if guide.has_previous_part?(part) -%>
	        <p class="previous"><a href="<%= guide_path(guide.slug, guide.part_before(part).slug) %>">&larr; Previous section</a></p>
	        <%- end -%>
	        <p class="pages">Part <%= part.number %> of <%= guide.parts.length %> in this guide.</p>
	        <%- if guide.has_next_part?(part) -%>
	        <p class="next"><a href="<%= guide_path(guide.slug, guide.part_after(part).slug) %>">Next section &rarr;</a></p>
	        <%- end -%>
	      </nav>
      </footer>

    </article>

  </section>

</div> <!-- end wrapper -->

</body>
</html>

